Must attach these policies to pullFromBucket-role-dis1w1hb to allow them to work.
This IAM role can be found in the lambda function > Configuration > Permissions > Execution Role > Edit

Must increase the memory up to at least 208 or greater, we only sent in some data, but need to increase more if we send more data through
